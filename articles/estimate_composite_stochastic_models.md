# Estimate composite stochastic processes

This vignette shows how to estimate composite stochastic models using
[`gmwm2()`](../reference/gmwm2.md). We generate data from known models,
fit composite candidates, and visualize the fits.

``` r
library(gmwmx2)
set.seed(123)
```
