# Inverse transform for power-law kappa

Maps domain values back to the real line using `atanh`.

## Usage

``` r
inv_trans_kappa_pl(x)
```

## Arguments

- x:

  Numeric vector in (-1, 1).

## Value

Numeric vector on the real line.
